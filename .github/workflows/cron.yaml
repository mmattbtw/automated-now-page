name: daily-cron
on:
  schedule:
    - cron: "0 1 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call API route
        run: |
          curl --request POST \
          --url 'https://yoursite.com/api/updatenowpage' \
          --header 'secret: ${{ secrets.API_SECRET_KEY }}'
